# Codeception Test Suite Configuration

actor: FunctionalTester
modules:
    enabled:
        - PhpBrowser:
            url: 'https://bank.%MAGIC_WORD%.com/en/login'
        - \Helper\Functional
        - REST:
            depends: PhpBrowser